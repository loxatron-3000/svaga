<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Manager" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="+" Click="AddUser_Click" Margin="5" Padding="10,5"/>
            <Button Content="Cancord" Click="DeleteUser_Click" Margin="5" Padding="10,5"/>
            <Button Content="Edit" Click="EditUser_Click" Margin="5" Padding="10,5"/>
        </StackPanel>

        <ListView Grid.Row="1" Name="UsersListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Email" Width="200" DisplayMemberBinding="{Binding Email}"/>
                    <GridViewColumn Header="Age" Width="60" DisplayMemberBinding="{Binding Age, StringFormat={}{0} y.o.}"/>
                    <GridViewColumn Header="Active" Width="60" DisplayMemberBinding="{Binding IsActive}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>